import 'package:flutter/material.dart';

const primaryColor = Color(0xFF00AEAE);
const primaryAccent = Color(0xFF00EFD6);

const whiteColor = Color(0xFFF1F1F1);
const blackColor = Color(0xFF111111);

// Orange
const motivationColor = Color(0xFFFFA500);

// Red Ribbon
const addictionColor = Color(0xFFEB102F);

// Blue
const depressionColor = Color(0xFF006FFF);

// Light Grey Border Color
const borderColor = Color(0xFFE9ECEf);

// Dark Grey Border Color
const borderColorDark = Color(0xFF343A40);
